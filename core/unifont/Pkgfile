# Description:
# URL:
# Maintainer:
# Depends on:

name=unifont
version=12.1.03
release=1
source=($name-$version.pcf)

build() {
	mkdir -p $PKG/usr/share/fonts/unifont
    cp $name-$version.pcf $PKG/usr/share/fonts/unifont/unifont.pcf
}
