# description	: LFS Core
# maintainer	: iagapie, igoragapie at gmail.com
# depends	    : filesystem linux-headers man-pages glibc tzdata zlib bzip2 xz file readline
# depends		: m4 bc binutils gmp mpfr mpc attr acl cracklib cracklib-words linux-pam shadow
# depends		: gcc pkg-config ncurses libcap sed psmisc iana-etc bison flex grep bash libtool
# depends		: gdbm gperf expat inetutils perl perl-xml-parser intltool autoconf automake kmod
# depends		: gettext elfutils libffi openssl python3 ninja meson coreutils check diffutils
# depends		: gawk findutils groff dosfstools libpng freetype efivar popt efibootmgr unifont grub
# depends		: less gzip zstd iproute2 kbd libpipeline make patch man-db tar texinfo vim nano
# depends		: systemd dbus procps-ng util-linux e2fsprogs pciutils sudo libarchive
# depends		: libtasn1 p11-kit make-ca wget curl dhcpcd libnl libxml2 wpa_supplicant
# depends		: linux-firmware cpio lz4 mkinitramfs git neofetch httpup scratchpkg scratchpkg-utils

name=core
version=1.0
release=1

build() {
	mkdir -pv $PKG/etc
}
