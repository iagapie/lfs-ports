# Description:
# URL:
# Maintainer:
# Depends on:

name=libexif
version=0.6.21
release=1
source=(https://downloads.sourceforge.net/libexif/libexif-$version.tar.bz2)

build() {
	cd $name-$version

	./configure --prefix=/usr --disable-static
	make
	make DESTDIR=$PKG install
}
