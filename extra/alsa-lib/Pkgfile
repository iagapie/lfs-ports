# Description:
# URL:
# Maintainer:
# Depends on:

name=alsa-lib
version=1.1.9
release=1
source=(ftp://ftp.alsa-project.org/pub/lib/$name-$version.tar.bz2)

build() {
	cd $name-$version

	./configure
	make
	make DESTDIR=$PKG install
}
