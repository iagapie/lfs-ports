# Description:
# URL:
# Maintainer:
# Depends on: popt

name=gptfdisk
version=1.0.4
release=1
source=(https://downloads.sourceforge.net/gptfdisk/gptfdisk-$version.tar.gz)

build() {
	cd $name-$version

	patch -Np1 -i ../gptfdisk-$version-convenience-1.patch
	make
	make DESTDIR=$PKG install
}
