# Description:
# URL:
# Maintainer:
# Depends on: python2

name=asciidoc
version=8.6.9
release=1
source=(https://downloads.sourceforge.net/asciidoc/$name-$version.tar.gz)

build() {
	cd $name-$version

	./configure --prefix=/usr --sysconfdir=/etc
	make
	make DESTDIR=$PKG install
}
