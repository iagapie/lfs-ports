# description	: -
# maintainer	: iagapie, igoragapie at gmail.com
# depends	    : xtrans libx11 libxext libfs libice libsm libxscrnsaver libxt libxmu libxpm libxaw
# depends		: libxfixes libxcomposite libxrender libxcursor libxdamage libfontenc libxfont2 libxft
# depends		: libxi libxinerama libxrandr libxres libxtst libxv libxvmc libxxf86dga libxxf86vm
# depends		: libdmx libpciaccess libxkbfile libxshmfence

name=xorg-libraries
version=1.0
release=1

build() {
	mkdir -pv $PKG/etc
}
