# Description:
# URL:
# Maintainer:
# Depends on: xwud

name=xcursor-themes
version=1.0.6
release=1
source=(ftp://ftp.x.org/pub/individual/data/$name-$version.tar.bz2)

build() {
	cd $name-$version

	./configure \
		--prefix=/usr \
		--sysconfdir=/etc \
		--localstatedir=/var \
		--disable-static
	make
	make DESTDIR=$PKG install
}
